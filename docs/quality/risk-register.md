# Risk register

> Living list of known project risks. Update when a risk is identified,
> mitigated, accepted, or proven false. Closed risks stay here for audit
> (mark `status: closed`), they are not deleted.

## Format

| ID | Risk | Likelihood | Impact | Mitigation | Status | Owner | Opened |
|----|------|------------|--------|------------|--------|-------|--------|
| R-001 | Anthropic 2026-06-15 split of `claude -p` from subscription leaves cc-proxy users paying Agent SDK credit | high | high | Accepted 2026-07-30 (ADR-0007): `claude -p` is now the only Claude runtime — the TTY subscription escape hatch was removed with Claude TTY mode; BILLING-001 guards only against background (non-turn) `claude -p` spend | closed | — | 2026-05-14 |
| R-002 | Scope-pruned traceability audit found untested safety-critical bypass, raw preview, IM, worktree, native-session, terminal, contract, git, and secrets flows | high | high | Use `docs/quality/traceability.md` Top 10 as the coverage backlog; start with SEC-012, FILE-003/SEC-009, IM-001/IM-003, WT-001/WT-003, NATIVE-001/ERR-011 | open | unassigned | 2026-05-17 |
| R-003 | Codex CLI runtime may lose PTY notifications through the shared codex-proxy routing layer; future Codex versions could also change 0-turn resume behavior | med | high | Eliminated 2026-07-31 (ADR-0008): Codex session TTY, `tty.*` routing, and the interactive resume runtime were removed | closed | — | 2026-05-20 |
| R-004 | Current Kimi ACP has no `session/close`: detached session resources remain until shared-process shutdown, while an unsafe idle restart could stop background work | med | high | Capability-gated detach, per-session facade release, single-flight shared-host startup, and no fixed restart are implemented; Phase 0 still needs RSS/background-task evidence before any globally-quiescent drain policy is activated; upstream the thin close handler | open | unassigned | 2026-07-28 |
| R-005 | A Gian-managed updater could activate a compromised or protocol-incompatible Claude/Codex/Kimi artifact, or race a vendor self-updater | med | high | Use official integrity data, immutable versions, structured compatibility probes, process leases, vendor updater isolation, and last-known-good rollback | open | unassigned | 2026-07-28 |
| R-006 | Electron loads daemon-owned localhost content; a port hijack or shell/host version skew could expose hostile content or break desktop startup | low | high | Keep Chromium sandbox + context isolation + no Node integration, allowlist configured origins, validate `/health`, keep preload IPC narrow, and add compatibility negotiation before independent auto-update ships | open | unassigned | 2026-07-28 |
| R-007 | Gian-managed and user-installed Kimi versions share `~/.kimi-code`; incompatible session-store versions could corrupt or strand native sessions | med | high | Gate managed activation on supported store versions, never auto-downgrade across unknown formats, keep immutable last-known-good binaries, and surface `DATA_VERSION_INCOMPATIBLE` rather than silently creating a session | open | unassigned | 2026-07-28 |
| R-008 | A Codex app-server upgrade could change effective permission response fields or `turn/start` permission precedence, breaking Custom restore or sending conflicting sandbox/profile fields | low | high | Keep named permission and raw sandbox mutually exclusive, fail closed when thread bootstrap omits effective policy, and run proxy contract plus real CLI compatibility probes before activation | open | unassigned | 2026-07-29 |
| R-009 | CLI upgrades may change token-usage or compaction event shapes; Kimi currently needs the human-readable `/status` fallback because ACP 0.30 does not reliably emit context usage | med | med | Keep usage adapters and version fixtures under contract tests, suppress hidden status output, recognize manual and auto compact boundaries, reject summarization/recompute samples, and show unknown instead of guessing when no authoritative sample parses | open | unassigned | 2026-07-29 |
| R-010 | Codex TTY was retained without a web entry point, leaving `runtime_mode` and `codex_chat_cli` as unreachable compatibility surfaces | med | low | Closed 2026-07-31 (ADR-0008): Codex TTY, the runtime API, and the unused preference were fully retired; migration 038 normalizes legacy rows | closed | — | 2026-07-30 |
| R-011 | Discord/Slack duplicate session, turn, and queue repositories could diverge from the Host state used by Web and executors | high | high | Closed 2026-07-31 (ADR-0009): platform managers use canonical Host services; migration 039 removed duplicated tables and outbox now references `sessions` | closed | — | 2026-07-31 |
| R-012 | TypeScript compilation could leave deleted modules in `dist`, or retain `tsbuildinfo` after deleting `dist`, allowing old code to run or a required entrypoint not to be emitted | high | high | Closed 2026-07-31: every emitted TS package removes `dist` and `*.tsbuildinfo` before build, proxy tests build from a clean tree, the dev-launcher test guards the Desktop clean contract, and CI-004 records the invariant | closed | — | 2026-07-31 |
| R-013 | A desktop GitHub OAuth token could leak through renderer IPC, plaintext disk state, broad scopes, or release misconfiguration | low | high | Main-process-only Device Flow; no requested scopes; no client secret; renderer receives only public profile; token encrypted through macOS safeStorage; release fails when `GIAN_GITHUB_CLIENT_ID` is absent; AUTH-002 tests pin these boundaries | open | unassigned | 2026-08-01 |
| R-014 | First-run setup can block if a vendor installer changes or the matching Gian Proxy asset is missing from GitHub Releases | med | high | Detect existing/configured CLIs before installing; use vendor-owned official installer URLs; version and SHA-256 verify immutable Proxy assets; require all Agent pairs ready before completion; keep setup re-runnable from Settings | open | unassigned | 2026-08-01 |

_(Add new rows as risks emerge. Likelihood / Impact: low / med / high.)_
