/** Context cards (folder / pasted text / browser element) — implemented in
 *  `@gian/chat-ui`, shared by the composer, the transcript, and the queue. */
export { ContextCards } from '@gian/chat-ui';
