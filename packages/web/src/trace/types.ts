/** The Web projection consumes the canonical Core Trace contract directly. */
export type {
  JsonValue,
  TraceEvidence,
  TraceItem,
  TraceItemKind,
  TraceItemShape,
  TraceSnapshot,
  TraceStatus,
} from '@gian/shared';
